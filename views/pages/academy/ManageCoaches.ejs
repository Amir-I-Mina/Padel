<!DOCTYPE html>
<html>
<head>
    <title>Manage Coaches</title>
    <link rel="stylesheet" href="../../../public/CSS/ManageCoaches.css">
</head>

<body>

<div class="navbar">
    <div class="logo">Yalla Padel</div>

    <div class="nav-links">
        <a href="/home">HOME</a>
        <a href="/academy/admin/coach-list">LIST OF COACHES</a>
    </div>
</div>

<div class="hero">
    <a href="/academy/admin" class="back">← BACK TO LIST</a>
    <h1>MANAGE COACHES</h1>
</div>

<div class="top-bar">
    <button id="addCoachBtn">+ Add Coach</button>
</div>

<div class="dashboardContainer">

    <% if (coaches && coaches.length > 0) { %>

        <% coaches.forEach(coach => { %>

            <div class="coach-card">

                <div class="menu">

                    ⋮

                    <div class="menu-options">

                        <button
                            class="edit-btn"
                            data-id="<%= coach._id %>"
                            data-name="<%= coach.name %>"
                            data-age="<%= coach.age %>"
                            data-phone="<%= coach.phone %>"
                            data-location="<%= coach.location %>"
                            data-experience="<%= coach.experience %>"
                            data-days="<%= coach.availableDays %>"
                            data-times="<%= coach.availableTimes %>"
                            data-type="<%= coach.trainingType %>">
                            Edit
                        </button>

                        <form action="/academy/admin/delete-coach/<%= coach._id %>" method="POST">
                            <button type="submit">Delete</button>
                        </form>

                    </div>

                </div>

                <h3><%= coach.name %></h3>

                <p>Age: <%= coach.age %></p>
                <p>Phone: <%= coach.phone %></p>
                <p>Location: <%= coach.location %></p>
                <p>Experience: <%= coach.experience %></p>
                <p>Days: <%= coach.availableDays %></p>
                <p>Time: <%= coach.availableTimes %></p>
                <p>Type: <%= coach.trainingType %></p>

            </div>

        <% }) %>

    <% } else { %>

        <p>No coaches available.</p>

    <% } %>

</div>

<div id="coachFormContainer" class="form-container" style="display:none;">

    <h2 id="formTitle">Add Coach</h2>

    <form id="coachForm" action="/academy/admin/add-coach" method="POST">

        <label>Coach Name</label>
        <input type="text" id="name" name="name">

        <label>Age</label>
        <input type="number" id="age" name="age">

        <label>Phone</label>
        <input type="text" id="phone" name="phone">

        <label>Location</label>
        <input type="text" id="location" name="location">

        <label>Experience</label>
        <input type="text" id="experience" name="experience">

        <label>Available Days</label>
        <input type="text" id="availableDays" name="availableDays">

        <label>Available Times</label>
        <input type="text" id="availableTimes" name="availableTimes">

        <label>Training Type</label>

        <select id="trainingType" name="trainingType">
            <option value="private">Private</option>
            <option value="group">Group</option>
        </select>

        <button type="submit">Save Coach</button>

    </form>

</div>

<script src="../../../public/js/ManageCoaches.js"></script>

</body>
</html>